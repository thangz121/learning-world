# W0-T1-UNITY-GREEN checkpoint: Unity project excludes.
# Tracked: Assets, Packages/manifest.json, Packages/packages-lock.json,
# ProjectSettings, docs (+ .github workflows). Everything else excluded.

# Generated / derived — never commit
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Mm]emoryCaptures/
[Dd]ataStore/

# Autogenerated IDE / VS project files (regenerate via Unity Preferences)
*.csproj
*.slnx
*.sln
*.user
*.userprefs
.vs/
.vscode/
.idea/

# Unity editor user state — machine-specific and regenerated automatically
[Uu]serSettings/

# OS noise
.DS_Store
Thumbs.db

# Phase 2.1-local: curated real speech samples are LOCAL ONLY (privacy:
# no raw voice in the repo, ever). Import via tools/import_speech_sample.py.
Content/speech_samples/

# Phase 2.1-local M6: phone-mic gateway secrets/recordings are LOCAL ONLY.
# TLS private keys must never be committed (§33). Opt-in test recordings
# (--record-dir) must never be committed (§20).
*.pem
*.key
*.crt
phone-mic-recordings/

# Nested scratch Unity project (not part of this checkpoint)
My project/
