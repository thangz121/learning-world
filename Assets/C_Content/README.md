# C_Content (Agent C)

Owns learning text + progression metadata. Text-vs-audio rule (Part K): C edits text + approval intent only; D owns generation/QA/audio files — C text change triggers D regen, never same-file audio edits.
